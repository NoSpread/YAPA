type IRDBOptions = {
    host: string,
    port: number,
    db: number,
}

type ISQLOptions = {
    host: string,
    user: string,
    pass: string,
    db: string,
    port: number
}

export {
    IRDBOptions,
    ISQLOptions
}